logging:
  level:
    org:
      springframework: debug
spring:
  jackson:
    serialization:
      write-dates-as-timestamps: 'false'
  security:
    user:
      name: username
      password: password
management:
  endpoints:
    web:
      exposure:
        include: '*'